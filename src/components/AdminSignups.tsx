import AdminRegistrationsOverview from './admin/AdminRegistrationsOverview'

/** Admin Enrollments — registrations by member or by program/class schedule. */
const AdminSignups = () => {
  return <AdminRegistrationsOverview />
}

export default AdminSignups
