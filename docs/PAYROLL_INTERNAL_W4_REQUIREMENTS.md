# Internal 2026 W-4 completion

The employee onboarding UI now supports internal W-4 capture, complete official form review, authenticated final signature, encrypted retention and a readable hard copy. Upload/provider receipt remains available. Native administrator review-to-election transfer now covers supported federal elections. Encrypted draft saving and resume are now available; the remaining requirements below are not all complete.

Sources checked September 12, 2026: [IRS Publication 15-T, electronic and substitute W-4 requirements](https://www.irs.gov/publications/p15t), [official 2026 W-4 and instructions](https://www.irs.gov/pub/irs-pdf/fw4.pdf). The retained template is `backend/payroll/forms/irs-w4-2026.pdf`, five pages, SHA-256 `92444d8856ce55d9e25dca8b6d1420634fc68b11e1ab1f760916ea29ddd312b2`. This pins the reviewed edition; a mutable remote URL must not silently change what employees sign.

Implementation requirements:

- Preserve the required face-of-form wording and all choices from Step 1(c) through Step 4(c), including the exemption section. Required wording must not be hidden by default or only shown in hover/pop-up content. Provide the complete official instructions/worksheets and the official link.
- Keep all three filing-status choices, Step 2(c), unrestricted whole-cent Step 3 totals (not only multiples of dependent credits), Steps 4(a)–4(c), the exemption checkbox/conditions and a nonresident-alien field. The 2026 form uses $2,200 in Step 3(a); do not carry older year wording into this form.
- For an exempt submission, the official instructions require personal information and signature only; contradictory completed steps need correction rather than silent deletion. Capture must not imply that an exempt or nonresident submission is automatically supported by every payroll calculation path.
- Preserve employee identity, answers, exact perjury wording, form/template revision, authenticated session, final signature and submission timestamp. Signature is the final entry; editing answers afterward requires a new review/signing step. Record submission accesses and retain evidence of the named employee's submission.
- Encrypt sensitive identity/form data with the existing scoped document vault. Do not put SSNs or complete forms in onboarding response JSON, ordinary audit messages, URLs, browser storage, or logs. A missing production vault key is a configuration issue, not permission to store plaintext.
- Produce and verify a hard-copy PDF containing the submitted information. Retain all five official pages. Validate both canonical AcroForm values and page widget appearances; inspect rendered output for clipping. Preserve the untouched source template.
- Bind a review intent to the exact payload and employee/onboarding cycle. Revalidate the employee session under the employer lock at signing, make retries idempotent, and reject stale intents. Signing, encrypted document creation, submission audit and task transition must be atomic.
- Link the submitted form to administrator review and downstream federal elections without fabricating a signature or treating an administrator's edit as the employee's response. Changed answers require an employee amendment. Existing uploaded forms remain usable during the transition.
- Test the entire browser journey, return/reload, amendment, response loss, concurrent signing, revoked session, cross-employee access, encrypted storage, PDF contents, and calculation-source changes. Verify the new form capture against the actual full onboarding journey.

Current evidence: `w4Form2026.js` validates the form's core identity/selection/amount contract and maps official fields. Three unit checks passed in `/tmp/payroll-w4-form-contract.log`, covering explicit filing choices, arbitrary credit cents, nullable blanks, exemption consistency, invalid input and the five-page template's fields. The parser inspected the official canonical fields and widget hierarchy. This is groundwork only: no native form screen, signed PDF, storage/submission transaction or automatic tax-election integration has been implemented yet.

Renderer progress (checkpoint 492): `w4Pdf2026.js` produces pinned five-page unsigned/signed-form representations with read-only canonical fields. Five unit tests and independent 51-widget QA passed; all rendered pages were inspected after fixing invisible checkmark appearances. This is a rendering primitive only. Signing authorization, encrypted storage and election activation remain unimplemented. The current standard font rejects unsupported Unicode rather than silently dropping characters; embed suitable fonts before enabling the flow for all names.

Current progress (checkpoints 493–494): The authenticated encrypted preview/signature endpoints and employee screen are now implemented. Signing requires the latest unexpired scoped preview, all five recorded rendered-page visits, the exact declaration and a final typed signature. Immutable records link the preview, session, signed PDF, task and audit. The final mobile browser journey and seven backend/workforce checks passed, and all five rendered canvas pages were visually inspected. These checks do not establish full production compliance or complete every requirement above. The UI uses pinned `pdfjs-dist` 5.6.205, following [Mozilla's PDF.js examples](https://mozilla.github.io/pdf.js/examples/); the worker is bundled locally and form data is not sent to an external viewer. Remaining work includes automatic admin election transfer, resumable encrypted drafts, worksheet calculations, non-Latin fonts and broader amendment/browser/production checks.

Current progress (checkpoint 495): Admin pay setup now derives read-only federal values from the retained signed W-4 and applies them with verified state elections after checklist review. Saving binds the current signed submission/document fingerprint; modified federal values and stale sources are rejected. Amendments mark old elections pending so current review and saving are required before reuse. The expanded browser handoff and 15 broader backend checks passed. The signed exempt form retains its required blank filing status; native calculator support for that shape remains unfinished, as do nonresident/lock-in exceptions and the other open requirements.

Current progress (checkpoint 496): Explicit encrypted draft saving and resume are implemented for incomplete answers, including across sessions. CAS revisions and retry keys prevent stale overwrites; signing clears the draft atomically and retains a revision tombstone. Employee-only endpoints return no-store responses and reject signatures in draft data. The expanded browser journey, four integration regressions and additional cross-scope/missing-key checks passed. Actual signing still requires the complete validated form, current preview, all page visits and final signature.

Current progress (checkpoint 497): Native exempt W-4 elections now keep their required blank filing status and produce zero federal withholding for supported current-year payment dates, while Social Security, Medicare and applicable state taxes remain calculated. Receipt date comes from the retained signing evidence and is bound to the source fingerprint. Earlier payments and unsupported years require another applicable certificate/treatment. Both ordinary and exempt browser journeys and 20 backend regression checks passed. Annual renewal/expiration workflows, historical certificate selection, broader supplemental combinations and nonresident/lock-in handling remain open. Sources rechecked: [IRS Publication 15, exemption from federal income tax withholding](https://www.irs.gov/publications/p15) and [Publication 505, claiming exemption](https://www.irs.gov/publications/p505). These explain exemption eligibility, continuing Social Security/Medicare withholding, effective receipt, annual renewal, and completing only personal information/signature on an exempt certificate. The pinned 2026 form states February 16, 2027 for renewal; cross-year automation is not yet implemented.

Checkpoint 498 (embedded W-4 name font): Replaced Helvetica field rendering with a digest-pinned, locally retained SIL-OFL Noto Sans Regular font and pinned @pdf-lib/fontkit dependency. Subset embedding preserves tested extended Latin, Vietnamese, Greek and Cyrillic names without an external font service. Missing glyphs fail before preview/signing instead of silently rendering boxes; this is not universal Unicode support. Six form/PDF checks passed, including canonical names and appearance presence. Both ordinary and exempt browser journeys passed with a Polish name through draft resume, preview, signature retry and admin election transfer (/tmp/payroll-w4-font-browser.log, 44.2 seconds). A mixed-script signed fixture was rendered and visually inspected; all 51 canonical/widget pairs agreed and all five official pages remained. Two serial database regression checks also passed for encrypted signing/retries and native election amendments (/tmp/payroll-w4-font-signature-regression.log). Scoped lint passed. Assumptions: preserve exact supported names, retain the original IRS template, and never transliterate unsupported names automatically. CJK, complex-script layout, worksheets, renewal/history and the broader onboarding/production requirements remain open. No production action or public push occurred.

Checkpoint 499 (internal multiple-jobs W-4 worksheet): Added the printed 2026 Step 2(b) two-/three-job worksheet inside employee onboarding. All 516 page-5 table cells across 43 rows are reproducibly extracted from the digest-pinned IRS PDF; the extraction script has a --check mode. Calculations sort annual taxable wages, perform both three-job lookups, show each worksheet line, use an explicitly entered highest-job annual pay frequency, round the final division to cents and add other requested per-check withholding. Explicit application requires confirmation that this is the highest-paying job, replaces Step 4(c), clears alternative Step 2(c), and invalidates stale calculation results when inputs change. Employees can download an unsigned text worksheet for their records; raw household wages remain in memory, while the applied result uses existing encrypted draft/signature storage. Four focused calculation checks passed, including all three filing tables, band/cents boundaries, the inclusive $120,000 limit, integer rounding and invalid inputs. Both ordinary/exempt browser journeys passed (55.2 seconds, /tmp/payroll-w4-worksheet-browser.log), including downloaded record contents and ordinary signing/admin transfer. A further ordinary browser check verified three-job results and encrypted draft resume of the applied amount (/tmp/payroll-w4-worksheet-resume-browser.log); the mobile worksheet screenshot was inspected. Scoped lint, table-source comparison, whitespace and final build passed (/tmp/payroll-w4-worksheet-build-final.log), with existing build warnings. Assumptions: annual taxable wages include concurrent spouse jobs for joint filers; no invented default pay frequency; no automatic transliteration or transmission of household wages; explicit replacement of the current extra withholding. Remaining: internal deductions worksheet, expanded tables for higher multiple-job wages/more jobs, year-to-date/partial-year estimator, renewals, state/I-9 native completion and broader production/onboarding requirements. No production action or public push occurred; the full goal remains active.

Checkpoint 500 (internal W-4 deductions worksheet): Added the page-4 2026 deductions worksheet to employee onboarding, with detailed single/separate/joint/surviving-spouse/head-of-household status matched to Step 1(c). The helper calculates qualified-estimate caps, senior amounts, medical/charity expense floors, state/local caps, itemized limitation, standard-deduction comparison and the final additional Step 4(b) amount. It does not enter the standard deduction itself as an additional deduction or count charitable gifts twice. Unsupported income/debt limitation cases stop with an explanation instead of silently omitting a claimed estimate; separate filers must confirm standard-deduction eligibility. Inputs are employee-provided eligible estimates; this is the printed annual worksheet, not a full eligibility/phaseout or tax-return engine. Applying explicitly replaces Step 4(b); changing worksheet inputs hides stale results. An unsigned text record is downloadable; raw estimates stay in memory and only the applied result follows encrypted draft/signature storage. Ten worksheet calculation tests passed (/tmp/payroll-w4-deductions-unit-final.log), including filing distinctions, caps, floors, limitation boundaries, the senior skip rule, equality selection, malformed input and integer overflow. Review caught and fixed a half-cent error: expense deductions now round after subtraction, with an explicit regression test. Both complete browser journeys passed (/tmp/payroll-w4-deductions-browser.log, 1.6 minutes), proving calculation/download, explicit application, stale-result handling, draft resume, signed PDF amount, signature retry and admin transfer of the $2,100 ordinary deduction; exempt fields stayed blank. Mobile result lines were visually inspected. Scoped lint, whitespace and final build passed (/tmp/payroll-w4-deductions-build-final.log), with existing warnings. Assumptions: blank optional estimates mean zero; employees review eligibility; dollar calculations use integer cents and round percentage results half up; detailed household estimates are not sent to admins. Sources: retained 2026 IRS Form W-4 page 4 and IRS Schedule 1-A for the stated married/SSN eligibility restrictions. Remaining: comprehensive eligibility/phaseout and year-to-date estimator, wider multiple-job cases, renewals/history, additional scripts/fonts, state/I-9 internal completion and broader onboarding/production requirements. No production action or public push occurred; the full goal remains active.

Sources checked September 12, 2026: https://www.irs.gov/pub/irs-pdf/fw4.pdf and https://www.irs.gov/pub/irs-pdf/f1040s1a.pdf (the latter is the 2025 Schedule 1-A, used for eligibility cautions, not to substitute 2025 amounts into the 2026 worksheet).
