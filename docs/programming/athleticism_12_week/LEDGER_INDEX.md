# Progression and workload record index

The complete outline and the detailed prescriptions describe different planning stages. Read them together; a blank numeric field in the preserved outline is not the current dose when a detailed entry exists.

| Record | Authority and use |
|---|---|
| `anchor_progression_ledger.json` | The audited Stage 2 map of 420 planned anchor/entry-branch opportunities. It fixes family roles, prior/next offered exposures and decisions. Its null numeric doses mean “not prescribed at outline stage.” |
| `workload_exposure_ledger.json` | The audited Stage 2 map of 60 main offerings and their adjacent-demand/attendance rules. No actual completion is inferred. |
| `detailed_anchor_ledger.json` | Current written exercise doses and set purposes for authored sessions. Day 1's six training anchors link back through `outline_anchor_key`; targeted preparation has its own entries. Instructional entries refer to their separate map session. Exact alternatives remain in the session and detailed workload scenarios. |
| `detailed_workload_ledger.json` | Recalculated age/mode/alternative scenarios for the authored exemplars. Select one eligible scenario, never add scenarios together. Main six-effort routes require actual compatible history. Mixed instructional groups explicitly remove a second brace bout where the slower alternative schedule requires it. |
| `prescriptions/standard_preparation.json` | Exact base doses, count units, contact categories, within-age overrides and complete group clocks. Sessions embed this reference so their exported prescription can be read independently. |
| `instructional_on_ramp/week_01/or_02_anchor_ledger.json` | Eight individually authored OR-02 task records with prior/current/next decisions, conditional source identities and unknown actual evidence. |
| `instructional_on_ramp/week_01/or_02_workload_ledger.json` | OR-02's 240 eligible age/mode/landing/support/history scenarios. An actual prior-one-set or unknown hip history selects its one-set hold, even when a second set fits the clock. |
| `instructional_on_ramp/week_01/or_03_anchor_ledger.json` | Eight OR-03 task records, exact local source pointers, selected alternatives and prior/current/next stance/stopping decisions. Actual work remains unknown. |
| `instructional_on_ramp/week_01/or_03_workload_ledger.json` | OR-03's 504 eligible age/mode/travel/stance/support scenarios. Walking, jogging, position and standing are recorded separately; split entries/holds reflect the selected history route. Known approach metres exclude unknown actual braking/exit/return distances. |
| `instructional_on_ramp/week_01/or_04_anchor_ledger.json` | Nine OR-04 task records, including the counted E0 whole-corridor orientation. Exact conditional source pointers and prior/current/next decisions retain distinct rhythm, walking and running results. |
| `instructional_on_ramp/week_01/or_04_workload_ledger.json` | OR-04's 480 eligible age/mode/preparation/travel/support choices. Whole-corridor walks, short starts, easy upright segments, runoff and returns remain distinct; compressed L has no extra E1 bout. |
| `written_session_index.json` | Inventory of the reviewed written athletic sessions and distinct ledger files. Counts are derived from saved files; shared exemplar ledgers are counted once. |

For an athlete, retrieve the latest productive actual exposure and any more recent lighter/review exposure. Select the current session's precise eligible dose, record a justified deviation if history differs, and preserve the next planned anchor decision. Planned exercises and expected results do not populate actual fields. No athlete histories have been supplied; actual completion, actual load and response remain unknown.

The exemplar ledgers are derived by `prescriptions/check_exemplars.py`; OR-02, OR-03 and OR-04 have separate session-specific checkers in `prescriptions/`. These checkers do not choose exercises or create subsequent days. They write their own ledgers only after the relevant numeric checks pass and include source fingerprints. The per-session ledgers preserve later work when an earlier exemplar is checked again. If an outline relationship changes, revise the outline and reconcile affected sessions before regenerating. Do not regenerate the Stage 2 outline merely to erase detailed work.

Contact categories stay separate: an intentional bilateral landing is one event involving two feet; ordinary steps/support contacts are different. Unknown timed jogging/ankling contacts remain null. Sprint target metres exclude runoff and return travel. Breathing practice is distinguished from moving-limb brace practice. Separate tumbling doses remain unknown until their precise program is available.

## OR-05 and the completed written Week 1 audit

OR-05 adds eight task rows in `instructional_on_ramp/week_01/or_05_anchor_ledger.json` and 1,152 conditional choices in `or_05_workload_ledger.json`, independently checked by `prescriptions/check_or_05.py`. Handling, throws, reach/hold alternatives, retrieval and strength are separate; current actuals remain unknown. At the Week 1 checkpoint, the detailed inventory contained49 task rows and2,508 daily scenario choices across six written athletic sessions (Day1 and OR01–05). These counts exclude the separate weekly aggregate audit.

Read `instructional_on_ramp/DETAILED_OUTLINE_RECONCILIATION.json` together with the preserved Stage2 map. Its named amendments supersede affected fields: implemented OR05 preparation handling instead of a preparation throw; required OR06 first-flight instruction with retained unloaded hinge; and actual-branch carryover to OR15/19. At that checkpoint, the future amendments were not completed prescriptions; subsequent OR-06 status is recorded below.

`instructional_on_ramp/week_01/week_01_attendance_workload_ledger.json` records31 attendance subsets,18,744 mixed-booking/mode/age/hypothetical-hip-history paths and186 scalar-envelope summaries. It resolves72 weekly-only OR01 walking choices from the existing prescription, includes32 mixed-return occupancy checks and preserves unknown actuals. Do not add these aggregates to daily scenarios or treat independent maxima as one prescription. `WEEK_01_REVIEW.md` contains the substantive scope and carryover audit; `week_01_check_results.json` binds current sources and daily-ledger provenance. The weekly checker is `planning/check_on_ramp_week_01.py`.

## OR-06 written lesson checkpoint

OR-06 adds eight task rows in `instructional_on_ramp/week_02/or_06_anchor_ledger.json` and 6,480 conditional named choices in `or_06_workload_ledger.json`. `prescriptions/check_or_06.py` checks all three ages, four modes, landing/hip routes, opportunity/repetition caps and support alternatives. Caps can share a physical dose; never add alternatives together. First-flight opportunities, conditional bilateral contacts, first/familiar implement handling and actual unknown outcomes remain separate.

At the OR-06 checkpoint, the detailed inventory was **57 task rows and 8,988 daily scenario choices across seven written athletic sessions: Day 1 and OR-01–06**. These totals exclude the separate Week 1 audit. The active outline amendment marks OR-06 implemented and individually reviewed; OR-15/19 remain future prescriptions. Retaining a known grounded task may accompany independently eligible hinge instruction. First flight keeps the hinge unloaded.

Week 1 was rechecked after this carryover-status clarification; its five prescriptions and 18,744 attendance/history paths are unchanged. The weekly checker validates its carryover contract without falsely requiring a later lesson to remain unwritten. OR-07 was the next individual lesson; its current evidence follows. No Week 2 audit is claimed before OR-10.

OR-06 uses a lossless schema-2 workload ledger to avoid repeating the same dose and timing objects thousands of times. Each named scenario retains its selector values; the file documents common fields and local JSON-pointer pools. Use `prescriptions.check_or_06.resolve_workload_scenarios(document)` after importing that checker from the prescriptions directory, or follow the saved storage contract, before accessing a resolved scenario. The checker verifies serialize/parse/resolve equality and matching canonical digests for all6,480 scenarios before saving. Earlier daily ledgers retain their existing schema.

## OR-07 written lesson checkpoint

OR-07 adds eight task rows in `instructional_on_ramp/week_02/or_07_anchor_ledger.json` and 4,320 enumerated timing scenarios in `or_07_workload_ledger.json`. Its checker also validates all252 reduction packets and all16 hold/hip pairings per scenario, covering69,120 named combinations by a fixed-start reduction proof. The latter are not additional independently simulated timelines and are not added to the inventory total. Forty-four adverse probes reject invalid models and missing prerequisites.

At the OR-07 checkpoint, the detailed inventory was **65 task rows and 13,308 daily scenario choices across eight written athletic sessions: Day 1 and OR-01–07**. One additional inspected local teaching mapping brought the mapping-record total to38; current canonical approvals remained unverified. The separate Week1 audit retained18,744 paths and186 summaries.

OR-07 uses the shared schema-2 lossless resolver exposed by `prescriptions/check_or_07.py`. A resolved scenario distinguishes its longest permitted `clock_reference_doses` from the complete selectable `dose_options`. Choose actual-history-compatible holds/repetitions; a timing envelope never authorizes restoring a smaller recent dose. The checker verifies exact serialized round-trip equality for all4,320 scenarios. Directional opportunities, ordinary returns, actual unknown contacts and reduced strength doses remain separate.

The explicit OR-07 amendment is implemented and individually reviewed. It preserves full preparation plus90/90-second targets, straight-stop P2 and the first counted exit in E1; stationary E1 stays in separately visible personal bays. OR-06 and Week1 checks were refreshed because the shared amendment changed; their physical prescriptions and scenario digests remain unchanged. The Week1 checker accepts uniquely named valid later amendments without granting those lessons its own PASS.

OR-08's bounded source/stimulus review in `planning/OR08_STIMULUS_AND_SOURCE_REVIEW.md` preceded its individual prescription. Its completed daily evidence follows; no Week 2 review is completed by either daily lesson.

## OR-08 written lesson checkpoint

OR-08 adds nine task rows in `instructional_on_ramp/week_02/or_08_anchor_ledger.json` and 6,528 enumerated timing scenarios in `or_08_workload_ledger.json`. Its independent checker validates 144 reduction packets, including 18 explicitly unavailable L-mode DB packets, and all 32 knee/hip/support selections within each scenario. The resulting 208,896 named factor choices are covered by a fixed-start reduction proof and are not added to the scenario total. All 550 mixed P2 timing sequences and 45 adverse probes pass.

At the OR-08 checkpoint, the detailed inventory was **74 task rows and 19,836 daily scenario choices across nine written athletic sessions: Day 1 and OR-01–08**, held in 16 distinct daily ledger files. One additional inspected mapping record under the existing easy-run teaching key brings the mapping-record total to 39. This is a complete proposed card with easy and modest-effort profiles; current canonical approval remains unverified. The separate Week 1 audit retains 18,744 attendance/history paths and 186 summaries.

Use `prescriptions/check_or_08.py`'s `resolve_workload_scenarios` function to resolve the schema-2 workload ledger. Exact serialize/parse/resolve equality passes for all 6,528 scenarios. Select a compatible dose from the saved options using actual history; `clock_reference_doses` are upper timing allowances. Count the whole-route E0 walk, independent P2 task, main running targets, runoff, returns, handling and strength separately. Actual contacts, load and response remain unknown.

The active OR-08 amendment is implemented and individually reviewed. First modest running uses repeatable easy-route evidence; retained modest running uses repeatable direct evidence without reconstructing an older easy record. Both require current response and runoff suitability. The lesson preserves recent smaller doses and any more recent actual hip unloading. OR-06, OR-07 and Week 1 were rechecked after the shared amendment changed; their physical prescriptions, scenario counts and resolved daily scenario digests remain unchanged.

OR-09 was individually authored after its reviewed source/stimulus brief. Its current daily evidence follows; the complete Week 2 audit still follows OR-10.

## OR-09 written lesson checkpoint

OR-09 adds nine task rows in `instructional_on_ramp/week_02/or_09_anchor_ledger.json` and 7,776 enumerated timing scenarios in `or_09_workload_ledger.json`. The independent checker validates 144 reduction packets (18 unavailable L-DB zero packets), all 32 knee/hip/support selections within every cohort and all 216 mixed triad patterns. The resulting 248,832 named factor choices are not added to the scenario inventory. All 52 adverse probes and six positive evidence-stage checks pass.

At the OR-09 checkpoint, the detailed inventory was **83 task rows and 27,612 daily scenario choices across ten written athletic sessions: Day 1 and OR-01–09**, in 18 distinct daily ledger files. One additional inspected record under the existing chest-projection teaching key brought the mapping-record total to 40. Its complete proposed known-release/GO-NO-GO card passed source/schema review; human canonical identity resolution and current release remain unverified. The separate Week 1 audit retained 18,744 attendance/history paths and 186 summaries.

Resolve the 12.5 MB schema-2 workload ledger with `prescriptions/check_or_09.py`'s `resolve_workload_scenarios` function. Exact serialize/parse/resolve equality passes for all 7,776 scenarios. The ledger separates E0's known-cue rehearsal, remaining E1 cue prefixes, intended releases, HOLD, handling and actual unknown outcomes. Timing-reference strength packets do not override smaller actual-history options. Its evidence chronology distinguishes first counted floor-access/handling instruction, entry to E0 and current E0 suitability before later choice work.

The active OR-09 amendment is implemented and individually reviewed: full base plus 40/140-second targets; zero preparation throws; one counted E0 within total main opportunities; only remaining E1 choices; explicit 90-second triads and serial incoming staging. OR-06, OR-07, OR-08 and Week 1 were rechecked after the shared amendment changed. Their physical prescriptions, scenario counts and resolved daily scenario digests remain unchanged.

OR-10 was individually authored after `planning/OR10_STIMULUS_AND_SOURCE_REVIEW.md`. That reviewed brief identifies the later exact goblet-squat candidates in migration 447 and preserves actual short-start and recent knee/hip/support history. Its completed daily evidence follows; the brief alone grants no workout completion or approved card.

## OR-10 written lesson and current inventory

OR-10 adds nine task rows in `instructional_on_ramp/week_02/or_10_anchor_ledger.json` and 3,888 conditional clock cohorts in `or_10_workload_ledger.json`. Its checker verifies 390 reduction packets and 128 combinations of main-count, knee-count, hip-count and support-level choices per cohort: 497,664 factored choices. These are bounded comparisons at fixed starts, not additional separately simulated daily timelines. All 2,777 mixed traffic sequences, 52 rejecting probes and 12 positive evidence-stage cases pass.

The current detailed inventory is **92 task rows and 31,500 daily scenario choices across eleven written athletic sessions: Day 1 and OR-01–10**, in 20 distinct daily ledger files. The new source review contains 13 inspected mapping records, bringing the cumulative record count to 53. This counts reviewed records across documents, not 53 unique exercise identities or new cards. The exact local dynamic goblet identity already exists; no new proposal was needed. Current canonical release remains unverified. Week 1 still has its separate 18,744 attendance/history paths and 186 summaries; Week 2 is not yet audited.

Resolve the 6.94 MB schema-2 workload ledger with `prescriptions/check_or_10.py`'s `resolve_workload_scenarios`. Exact serialized round-trip equality passes for all 3,888 scenarios. Choose from the saved actual-history dose options; clock-reference packets do not restore older doses. `movement_totals` contains summed totals without a misleading `_per_set` suffix; individual raw doses retain their per-set units. The final resolved-scenario digest is `1e8040430a339694ed12366689194cec5738e2d3eee24eca7c84eb790a76b1d4`.

The OR-10 amendment is implemented and individually reviewed. Full preparation and exactly two targets remain; E0 consumes the first total main opportunity. Lower technical intent remains available after L-to-D, while technical 20 m and easy 15 m routes stay distinct. First, retained, incremented and reintroduced goblet handling have separate evidence rules and preserve recent repetition caps. Static supported knee work does not overwrite bilateral squat history. Two directly coached knee stations depend on actual fixed DB/cradle fit for assigned users; arbitrary swaps require time and later omissions. OR-06–09 and Week 1 were rechecked after the shared amendment changed; their physical prescriptions and resolved daily digests remain unchanged.

**Next: the full written Week 2 audit.** Follow `planning/ON_RAMP_WEEK_02_AUDIT_BRIEF.md` before OR-11 detail. Include all five lessons, mixed attendance/bookings/modes, bounded Week 1 carry-in states, actual smaller-dose and handling histories, cumulative upper/lower-body work, and Week 3 carryover. No operational release, actual athlete result or precise separate tumbling is implied by this daily checkpoint.

## Instructional Week 2 — separate weekly aggregation

`instructional_on_ramp/week_02/week_02_attendance_workload_ledger.json` records 84,348 paths across 31 nonempty attendance subsets, 3 ages, all 4 mode assignments and 9 declared hypothetical carry-ins, with 837 saved summaries and 337,500 checked transitions. Complete daily scalar alternatives, exact selected witness paths, dose overlays, full preparation bouts/units and fixed-weekday examples remain separately identified. Unknown prior doses are not invented completed records; actual athlete/tumbling data remain null. Independent scalar endpoints are not one joint prescription.

The checker is `planning/check_on_ramp_week_02.py`; results, source review, substantive review and independent critique are beside the ledger. Current daily canonical digests and source bindings pass, as does weekly serialization equality. Weekly paths are not added to 31,500 daily scenarios or 92 task rows. The index still contains 11 written athletic sessions and 20 daily ledger files, with two separate weekly reviews: 103,092 combined weekly paths and 1,023 summaries. Read each week's own bounded state coverage before comparing counts.

## OR-11 — separate clock, direction and dose catalogs

Resolve instructional_on_ramp/week_03/or_11_workload_ledger.json with the shared schema-2 resolve_workload_scenarios function exposed by prescriptions/check_or_11.py. The root catalog contains 1,152 clock scenarios. Its nested direction_context_plans catalog separately contains 1,950 direction/context/first-side/count plans, and independent_reduction_checks contains 1,602 complete smaller-dose factors. A nested direction choice replaces its numbered E1 opportunity; it never adds another set. Lossless round-trip verification covers the complete combined 2,404,960-byte document.

The companion anchor ledger has eight task rows, with actual prior/selected/completed work null. P1/P2 and the full base remain counted; static stance holds/entries, dynamic repetitions, handling, approaches, assessed walking exits and ordinary returns use separate units. Unknown main/stance history stays null and selects only its independently eligible current cap. Natural contacts, actual kg and separate tumbling remain unknown.

The index now contains 12 written athletic sessions (Day1 and OR01–11), 100 task rows and 32,652 daily clock scenarios, with 22 daily ledger files. The two weekly reviews retain their separate 103,092 paths/1,023 summaries. OR11 adds one inspected mapping-record snapshot, resolving 14 reused/proposed keys for its review; the project total of 54 inspected record snapshots does not mean 54 unique or approved canonical exercises.
