# OR-09 — written athletic design review

**PASS for the written athletic lesson.** The [coach session](or_09.md) and [structured prescription](or_09.json) are independently reviewed for programming coherence. Current canonical approval, actual facility execution, athlete outcomes and precise separate tumbling remain unverified.

OR-09 teaches an early THROW/HOLD choice on a genuinely retained stationary chest projection. One known-cue E0 rehearsal consumes the first main opportunity; E1 uses only the remaining prefix. Total reference opportunities remain 3/2/2/1 for standard D/L and compressed D/L, before actual smaller caps. This preserves the prior opportunity limit while allowing intended throws to decrease when HOLD is selected.

## Findings and resolution

| Review finding | Resolution |
|---|---|
| A familiar rehearsal added before the full choice count would increase total work. | E0 is the first existing main opportunity. E1 is only 2/1/1/0 before cap1/2. Missing cue types do not generate extra opportunities. |
| The preparation outline requested a complete familiar throw, while P2 only handled the ball. | The active amendment retains the full base and 40/140-second position/handling targets, with zero preparation throws. The full known-cue rehearsal and retrieval are counted E0 within the main allocation. |
| HOLD needs its own handling and set-down clock. | Its 15-second action contains 2 s pickup, 2 s readiness/cue, 3 s chest hold, 5 s own-rack set-down and 3 s settle. THROW retains its 10-second action. HOLD has no intended release or retrieval. |
| The initial triad did not account for incoming athletes reaching their racks. | A serial 75–90 s staging window follows outgoing rack/queue clearance. First-triad staging is inside opening setup; later-round first-triad staging uses the final 15 s before the next round. No crossing of an active target lane is assumed. |
| Inherited E0 prose assumed three released balls in every group. | All actions must stop, all actually released balls settle and all retained balls be parked. Only released-ball owners retrieve after whole-group all-clear. |
| Choice packets inherited an unconditional 15 s retrieval value. | Their retrieval field is cue-dependent/null with an explicit response contract: THROW has owner retrieval; HOLD and empty prefixes have none. Actual false releases remain recorded separately. |
| Inherited DB timing named the old S2 hip role. | The model now references S4 and its fixed groups, matching push/pull/knee/hip/brace order. |
| Metadata and first-known-throw gates needed to be explicit. | Week 2, offering day 4 and instructional phase are saved. First known throwing uses complete component gates without demanding a prior successful throw. It cannot establish prior repeatability for choice from one E0 result. |

The independent critic verified all nine tasks, the resolved preparation base, age/mode alternatives, source-versus-competency distinctions, opportunity prefixes, fault handling and actual recent strength history. It returned scoped written PASS after the saved staging/model/amendment and S4 corrections. The separate source/proposal and numerical audits also passed within their stated scopes below.

## Numeric and source audit

The [independent checker](../../prescriptions/check_or_09.py) reports **7,776 enumerated timing scenarios, 52 rejecting probes and zero errors** in [the saved results](or_09_check_results.json). It checks 144 exact knee/hip/support reduction packets, including 18 explicitly unavailable L-DB zero packets. All 32 knee×hip×support selections are compared within every fixed-start cohort, covering 248,832 named choices without claiming that many separately expanded timelines. All 216 mixed triad patterns preserve one active action, parked retained balls, released-owner-only retrieval and serial incoming staging.

Six positive synthetic evidence checks distinguish first counted P2 handling, first known release without a prior whole-task record, E0 entry without requiring its future result, first choice without previous choice mastery and familiar small-dose DB history. Evidence chronology records what must be known before initial floor access, pickup, E0 and remaining E1 choice. A scheduled instruction never fabricates the actual observation it is meant to establish. Fault-count checks keep premature/incorrect releases in physical workload and canceled cues unobserved; no replacement opportunities are created.

The [source mapping](../../prescriptions/OR09_LIBRARY_MAPPING.md) and [complete proposed card](../../prescriptions/proposals/or09_chest_projection_teaching_candidate.json) passed the existing schema and source comparison: 17 source-reference groups, 11 reused keys, 15 links, 108 age/mode/cap/order selections (216 E0/E1 branches), 60 base strength rows and five inherited packet groups. Both cue contexts retain the local teaching key while human resolution of canonical profile-versus-card identity stays open. Current IDs, release approval, exact media and calibrated difficulty remain unset. The numeric audit independently compares both profiles’ total/cap/prefix release counts and 90-second staging clocks with the saved session.

[Nine progression rows](or_09_anchor_ledger.json) preserve task purposes and prior/current/next decisions. The [12.5 MB workload ledger](or_09_workload_ledger.json) passes exact serialize/parse/resolve equality for all 7,776 scenarios. Resolve schema 2 with `check_or_09.resolve_workload_scenarios`: upper `clock_reference_doses` prove timing, while `dose_options` supplies compatible actual-history selections. `planned_main_actions` separates known/choice cues, intended release, HOLD, handling and retrieval. Actual outcomes stay null; alternatives are never summed as one workout.

The model uses 90-second triads with sequential individual starts at 0/15/30 s, shutdown at 45–50, released-owner retrieval at 50–65, outgoing rack/queue clearance at 65–75 and next-triad staging at 75–90. Five triads form a 450-second round; corresponding rounds start 540 seconds apart. The last standard reference round clears at 44:30, and compressed at 30:30. These are conditional operating allowances, not proof of actual fit or a required child movement pace.

## Carryover and remaining limits

First choice instruction requires actual repeatable same-demand throwing and known-release/retrieval behavior, understood signals and suitable current E0 response. It does not require previous mastery of the whole choice task. Known-cue instruction remains available when the throw is emerging. One E1 cue observes only that presented rule; cap1 and compressed L provide E0 only. False releases count as physical throws and consume their opportunity; canceled cues remain unobserved and no attempt is repaid.

Strength retains five roles and recent actual setup, load, range and smaller counts. Familiar two-DB hip work needs its actual independent handling and equipment contract; more recent unloading governs over an older loaded record. Neither cue success nor attendance restores repetitions or load.

OR-13 requires its independently qualified cue/movement branch. OR-17 observes the actual retained rule with reduced prompting. A passed early-release rule may remain usable for REACT-T while HOLD is emerging, with the separately passed throwing/retrieval gates; neither result grants reactive cutting or landing competence.

Week 2's complete review follows OR-10. The final window has no extra physical sets. The separate 30-minute Body Control/Tumbling content remains unresolved; its handoff is not a completed prescription. This lesson does not complete the 60-session main block plus separate 20-session on-ramp.
