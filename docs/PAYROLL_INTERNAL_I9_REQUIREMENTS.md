# Internal I-9 implementation requirements

Checkpoint 524 (unresolved E-Verify continuation): The current recorded outcome, next action and admin-supplied review date are visible immediately when the case evidence view opens. Editing case details or replacing the PDF resets review confirmations. Pending-SSN/case-not-created records reject contradictory case references or closed-case claims. The full onboarding/payroll goal remains active.

Verification: 14 backend/engine tests passed without skips in 7.8 seconds (/tmp/payroll-everify-pending-tests.log). Both browser journeys passed in 50.3 seconds (/tmp/payroll-everify-pending-browser.log). The alternative journey now follows an authorized result with a case-in-continuance correction, verifies that changing evidence text clears all confirmations, retains the second event without overwriting the first, returns the task to IN_PROGRESS, reloads the page and confirms that the current review date/action remains visible. The reopened mobile summary was visually inspected. Lint, whitespace and production build passed (/tmp/payroll-everify-pending-build.log), with existing browser-data/chunk warnings and opt-in prerender skipped. The full payroll suite was not rerun.

Assumptions: the latest immutable event supplies the visible next action; its review date is entered by the reviewing admin, not presented as a calculated statutory deadline. A record explicitly labeled case-not-created cannot also claim a case reference or closed case. Any edit to result evidence requires fresh review confirmations. Test dates and case details are synthetic. Government submission, notices and employee decisions, referral tracking, final-nonconfirmation/resubmission disposition, receipt/reverification closure, Supplement B, corrections/retention and broader provider/production verification remain unfinished. No real case or production migration was performed.

Checkpoint 523 (evidence-backed E-Verify results): Admins can retain a reviewed case-result PDF and an immutable outcome history from the employee I-9 evidence view. Authorized closure requires explicit verification and retained evidence; unresolved outcomes remain in progress with a next action/date. Generic note-only closure is blocked. E-Verify follow-ups remain visible but no longer automatically block earned payroll. The full goal remains active.

The employee-scoped E-Verify result GET/POST routes retain encrypted case references, observation date, reviewed evidence note, confirmations, case-closed state and any next action. Each result has a private readable PDF, original examiner/task association, immutable revision and request hash. Concurrent/uppercase retries return one result; changed or stale saves are rejected. Audit failure rolls back the result, private file and task update. Authorized-and-closed evidence completes the follow-up; a later unresolved correction reopens it. Final nonconfirmation is recorded but remains in progress for an explicit disposition workflow. No employment status is changed. The generic compliance API cannot complete or waive these tasks, and a database guard requires the latest retained authorized result. The admin UI shows history/downloads and accepts a reviewed result with a PDF up to 5 MB; all confirmations begin unchecked and reset after recording.

Payroll correction: an open E-Verify follow-up had inherited the global critical-compliance payroll blocker. It now remains a critical visible warning without automatically withholding earned pay. Other critical compliance blockers retain their existing behavior. This separates case follow-up from payment of wages; it does not decide future employment eligibility.

Source review: official search results for https://www.e-verify.gov/e-verify-user-manual-40-case-resolution/41-close-case distinguish authorized automatic closure from other results. https://www.e-verify.gov/employers/verification-process/tentative-nonconfirmations/dhs-and-ssa-mismatches states that employers must not withhold/lower pay or take other adverse action because of a pending mismatch or continuance. Direct manual/PDF retrieval returned 403. This is not a complete verification of all current 2026 rules, deadlines, alerts or web-services requirements.

Verification: 18 serial backend/engine/route tests passed without skips in 8.8 seconds (/tmp/payroll-everify-results-final.log), including required confirmations, pending next actions, generic/database closure rejection, audit rollback, encrypted references, filename minimization, exact/concurrent/uppercase retries, stale requests, immutable history, correction reopening, foreign-facility isolation and nonblocking E-Verify payroll warnings while unrelated blockers remain. Both signing browser journeys passed in 52.9 seconds (/tmp/payroll-everify-browser.log); the alternative journey additionally records authorized-result evidence, recovers a simulated lost response to one event, shows COMPLETE and retains result history. The result form was visually inspected at mobile width. TypeScript, scoped lint and whitespace passed. The production build passed (/tmp/payroll-everify-build.log), with existing browser-data/chunk warnings and opt-in prerender skipped. The full payroll suite was not rerun. No production migration or real case submission occurred.

Assumptions and remaining work: administrators attest that evidence matches the official case and this employee/employer; the app has not connected to government case services. Pending-SSN evidence can omit a case number; other outcomes require a reference. Review dates are explicitly entered and are not asserted to be calculated statutory deadlines. Pending/final-nonconfirmation/resubmission dispositions, referral notices, employee notice/decision tracking, provider integration, complete case lifecycle, receipt/reverification closure, Supplement B, corrections/retention and broader payroll/provider/production verification remain unfinished. PDF evidence is checked for readability, not for truth of the reported government result. No payroll or employment decision is inferred from a pending result.

Checkpoint 522 (retained I-9 evidence and follow-up navigation): Hiring admins can reopen signed employer findings, distinguish current from historical certifications, download the signed form and selected copies, and see associated compliance tasks. Employee-specific compliance tasks link back to the correct onboarding workspace. This connects retained evidence to follow-up work; it does not complete receipt/reverification or E-Verify case-resolution workflows.

The admin-only employer-records endpoint checks employee/facility scope, decrypts immutable signature evidence using the original examiner association, compares retained review/submission/document hashes to their records, and returns findings with signed-document/copy references and associated task status. Reads receive no-store responses and metadata-only audit records. Current labeling requires the completed employer and employee steps, matching cycle, employer signature and employee submission; reopening review preserves the record with a historical label. The UI shows identity/authority findings, actual examination and signing dates, business calendar, late explanation, alternative qualification/video evidence, document acceptance/rule/notation details, recorded follow-up decisions and separate current task status. Downloads use the existing private-document route and retained filenames. Employee-specific Compliance entries navigate to the selected employee’s onboarding page.

Verification: five focused backend tests passed without skips in 8.9 seconds (/tmp/payroll-i9-records-tests.log), covering signature/current-history retrieval, foreign-facility rejection, metadata-only read audit and neighboring signing/route checks. Both physical and alternative signing browser journeys passed in 42.6 seconds (/tmp/payroll-i9-records-browser.log), including reopening retained findings, signed-PDF downloads, separate OPEN E-Verify status and navigation back to onboarding. The downloaded alternative-procedure PDF was rendered and visually inspected: the employee and employer signatures/dates are legible, document entries remain in List A, and the alternative box is checked. Scoped lint and whitespace passed. The production build passed (/tmp/payroll-i9-records-build.log), with existing browser-data/chunk warnings and opt-in prerender skipped. The full payroll suite was not rerun. These are synthetic records; no real examination, signature or external case submission occurred.

Assumptions and remaining work: hiring admins authorized for payroll management can review retained employer evidence across examiners, while unfinished examiner drafts remain private to their author. Historical evidence remains available without asserting current readiness. This view is not the full inspection/export package. Generic compliance-note closure still lacks dedicated receipt/reverification/E-Verify outcome evidence enforcement; those closure workflows, Supplement B, corrections, retention, current-rule verification and broader payroll/provider/production work remain open. No production migration was run.

Checkpoint 521 (secure examiner save/resume): Admins can save unfinished I-9 examination findings and resume them after preparing a fresh review of unchanged evidence. Drafts are encrypted and private to the authoring admin, protect against stale saves, and invalidate when the employee/employer basis changes. Signatures and certification confirmations are excluded from saved drafts and must be entered again. The full onboarding/payroll goal remains active.

The examination-draft GET/POST routes require the current scoped employer review and use no-store responses. Encryption binds facility, employee, employer task/cycle and admin. The stable draft basis includes employee submission, employer draft revision and preparer fingerprint, so renewing an unchanged preview preserves findings but does not preserve page visits. Partial dates/text and unanswered choices can be saved. Unknown fields, signatures and attestation booleans are rejected. Revision checks and unchanged/case-insensitive request retries prevent conflicting overwrites. Audit failure rolls back storage, and database guards enforce scope, revision advancement and current employee/employer evidence. Another admin receives their own empty draft. Saved draft text is absent from audit summaries and browser storage.

Verification: ten final serial backend tests passed without skips in 35.9 seconds (/tmp/payroll-examination-draft-tests-final.log): partial-input boundaries, encryption, concurrent/case-insensitive retries, stale saves, audit rollback, immutable scope, fresh-preview resume, changed employer invalidation, different-admin isolation, and neighboring packet/signing/route coverage. Physical and alternative browser signing regressions passed (19.3 and 16.3 seconds, /tmp/payroll-examination-draft-browser.log). The draft-resume browser check initially failed because exact getByLabel lookup included restored textarea text; the semantic textbox locator resolves the restored value correctly. Its final run passed in 22.2 seconds (/tmp/payroll-examination-draft-browser-resume.log), including simulated lost-save recovery to one revision, page reload, restored notes/day choices, blank signature/unchecked attestations, no retained signature and no private browser-storage text. The resumed mobile form was visually inspected. Scoped lint/whitespace and production build passed (/tmp/payroll-examination-draft-build.log), with existing browser-data/chunk warnings and opt-in prerender skipped. No full payroll suite rerun or production migration occurred.

Assumptions and remaining work: examination findings belong to the examiner who authored them; sharing an employer document draft does not authorize sharing that examiner's findings or consent. Any employer draft revision requires fresh findings even if an edit appears minor. Save is explicit, with no silent signing or browser persistence. Drafts are retained encrypted rather than destructively deleted; full retention policy, lifecycle/correction workflows, guest representatives, identity assurance, receipt/extension follow-up closure, Supplement B, inspection exports, current-rule verification and broader payroll/provider/production gaps remain open. Existing signature evidence is separate and immutable. No real examination, signature or external case submission occurred.

Checkpoint 520 (admin employer I-9 examination and signing): The admin payroll page now collects examination findings, selected document copies, business-calendar/deadline evidence, document acceptance and follow-up decisions, alternative-procedure attestations, and the examiner’s own signature. Physical and alternative signing browser journeys passed, including lost-response recovery, one retained signature, completion after reload and an OPEN E-Verify case-review task for the participating employer. The full onboarding/payroll goal remains active.

The preview response supplies the exact employer attestation, actual server date, current hire/offer dates, E-Verify participation and reviewed representative/method. No examiner confirmation is prechecked. Admins select the retained copies, confirm completeness and acceptance, and record exception/follow-up evidence where applicable. Document acceptance changes clear hidden exception fields. Signing uses the existing immutable transaction; unchanged retries reuse their request key. Completion refreshes the hiring checklist and removes the unsigned employer draft. The generic completion button is disabled for native employee-signed I-9s; backend and database enforcement remain authoritative.

Verification: four serial browser journeys passed in 1.1 minutes (/tmp/payroll-employer-ui-browser-final.log): current employee/preparer packet review, copy upload/view/reuse, physical employer signing, and alternative signing. Both signing journeys review all main/copy pages, enter examination evidence, simulate a lost successful signing response, retry to the same single signature, reload to COMPLETE and verify no examination/signature text in browser storage. Alternative signing leaves exactly one OPEN E-Verify case-review task; physical nonparticipating signing creates none. Both mobile forms were visually inspected and fit their containers without horizontal overflow. The first browser run stopped on an exact label selector for a select control; semantic combobox locators resolved it. This was followed by a passing physical run and the four-journey final run. Playwright was used because agent-browser is unavailable.

Final backend verification: 13 tests passed without skips in 35.9 seconds (/tmp/payroll-employer-ui-backend-final.log), covering examination validation, employer signatures, packet/copy regressions and route boundaries. Scoped lint and whitespace passed. The production build passed (/tmp/payroll-employer-ui-build-final.log), with existing browser-data age/chunk warnings and opt-in prerender skipped. The full payroll suite was not rerun.

Assumptions and limits: the employer confirms its actual business calendar; the named examiner supplies their own findings and signature; receipt/extension rules remain human-verified against the retained form notation. Examination entries currently remain only in component memory until signing, with an explicit warning; encrypted save/resume for these findings is still required. Refreshing the copy selection list after upload is currently explicit. Guest authorized-representative access, identity assurance beyond the authenticated-admin evidence, complete receipt/extension follow-up closure, Supplement B, corrections, retention/inspection exports, current-rule verification, broader names/scripts, and provider/production onboarding remain open. No production migration, real employee signature, real examination or external case submission occurred.

Checkpoint 519 (employer I-9 certification backend): Added a transactional employer signing endpoint with current packet/copy page requirements, examiner attestations, encrypted immutable signature evidence, exact retries and database enforcement against generic native-I-9 completion. Nine focused backend tests passed without skips. The admin examination/signing interface, browser verification, follow-up completion and broader payroll/provider/production work remain unfinished. This is a backend checkpoint, not end-to-end onboarding completion.

The examiner must explicitly confirm their identity/authority, document choice, physical presence or alternative-procedure eligibility, selected complete copies, and acceptance/follow-up decisions. The server checks actual examination dates, the confirmed business calendar, late-completion explanation, documented exception notation, and finite authorization follow-up. Special receipt/extension acceptance remains examiner-owned and requires an official rule reference; this is not a comprehensive automated legal rule engine. Signing retains the server's actual date and changes only employer signature/date fields in the reviewed PDF. The original employee and preparer evidence remains retained.

Assumptions: admins retain copies of all presented documents; the examiner confirms actual business days and closures; exception notations must already appear in the reviewed form and include examiner initials/date; exact signing retries return their historical receipt without claiming current readiness. Document follow-ups and participating-employer E-Verify work are created as OPEN tasks, not represented as completed cases. Alternative qualification is an attributable human attestation, not a live E-Verify verification. No production migration, real examination, real signature, case transmission or payment occurred.

Verification: /tmp/payroll-employer-signing-regression.log reports nine passed, zero failed/skipped in 28.0 seconds. Coverage includes missing packet/copy pages, generic completion bypass, audit-failure atomic rollback, concurrent/case-insensitive retries, changed request rejection, immutable signatures, retained PDF text-field preservation, business calendars, late completion, alternative qualifications, exception sources/notations, receipt timing and authorization follow-up. Neighboring employer packet/copy tests passed. UI, visual signed-PDF inspection, follow-up transaction branches and full-suite verification remain outstanding.

Checkpoint 518 (internal I-9 document copies): Added admin-scoped employer copy list/upload/view/page routes. Uploads require the current review, retain encrypted PDF/PNG/JPEG bytes under the employer task, replace original filenames with neutral generated names, and support exact/case-insensitive/concurrent request retries. PDFs are checked for readability, password protection and page count. The file type/size boundary is 5 MB per file; image display success is verified by the browser before its page visit is recorded. Files remain immutable and are not marked accepted by uploading or viewing them.

Every employer preview now retains immutable hashes of its normalized document rows. A copy binds employee submission, cycle, row key and that document hash. The API and database guards reject a page visit against a changed entry. A fresh preview of unchanged entries can reuse prior copies, while requiring fresh page-review visits; there is no need to upload the same bytes again merely because a preview was renewed. Separate private-file and copy records roll back together if the audit insert fails. The admin UI supports multiple copies per chosen document row, a multi-page PDF viewer, inline image viewing with zoom and horizontal scrolling, and refresh/reuse after reopening. Copy bytes and original names are not placed in browser storage or audit summaries. Shared source review/page freshness checks continue to require current employee and preparer evidence.

Verification: eight final serial backend tests passed without skips in 23.1 seconds (/tmp/payroll-i9-copies-tests-final.log), including copy encryption, exact/concurrent retry, unsupported/malformed PDF rejection, row/facility isolation, exact retained bytes, page bounds, immutable copy/page evidence, audit rollback, renewal/reuse, changed document-number rejection at API and database levels, and neighboring employer review/route checks. Two final browser journeys passed in 29.3 seconds (/tmp/payroll-i9-copies-browser-final.log): simulated lost upload response with one retained copy, two-page PDF review, image upload/render/review/zoom, reuse after reopening, no browser-storage copy content, no horizontal overflow or page errors, and the existing employer packet flow. The initial copy browser run finished its functional checks but closed the test server while a refreshed main-form page visit was pending; teardown now waits for that visit, navigates away and drains routes before closing the harness. The final rerun passed. The mobile copy viewer was visually inspected. Scoped lint, whitespace and production build passed (/tmp/payroll-i9-copies-build.log); existing chunk/data-age warnings and skipped opt-in prerender remain.

Source review: the official [DHS alternative-procedure notice](https://public-inspection.federalregister.gov/2023-15533.pdf) describes qualified E-Verify participation, consistent application, clear copies, live video examination and retention requirements. It distinguishes physical-examination copy rules for specified E-Verify documents from the alternative procedure’s all-document copy rule. USCIS and the E-Verify manual returned 403 during direct retrieval; search also produced impersonation domains, which were excluded. This 2023 notice does not establish verification of all current 2026 changes. No legal acceptance or remote qualification is inferred from a file upload.

Assumptions: multiple files can represent the required sides/pages of one chosen document; the 5 MB boundary applies per file; original filenames are unnecessary for retaining exact content; reviewers use the current packet when viewing a copy; unchanged document descriptions and employee submission permit byte reuse, with renewed review evidence. Completeness and acceptance of selected copies still need to be bound to an examiner’s own findings and signature. Employer examination inputs, copy-selection/completeness rules, alternative qualification, actual attestation/signing, receipts/extensions/deadlines, corrections/retention and broader payroll/provider/production work remain unfinished. No production migration, real examination or real signature occurred.

Checkpoint 517 (internal employer I-9 review packet): Added admin employer-preview and employer-page routes. Preparation requires an approved current employee Section 1, its original hiring-context revision, current saved employer draft, and the complete signed preparer roster with the retained admin completeness confirmation. It decrypts and verifies the original Section 1 PDF and each signed preparer supplement, composes Section 2 without employer signature/date, and encrypts the exact main PDF, normalized printed answers and separate supplement PDFs. Keeping supplements as separate retained documents preserves their canonical fields and original signatures. Review metadata binds employee submission, draft revision, basis hash, roster fingerprint and admin actor; reviews expire after 30 minutes and a later review invalidates an earlier one. Database guards enforce immutable review/page history, current source/draft/employee evidence and packet document membership. Page visits are separate for each main-form page and each preparer sheet. The shared currentI9EmployerReview helper is available for the future employer signing transaction, which must additionally validate examination evidence and signature intent.

The admin draft screen now prepares only saved entries, displays each packet document internally, clears the preview when entries change, and provides all eight official instruction pages. A browser failure exposed a real accessibility gap in the shared PDF viewer: PDF.js static text extraction omits filled widget values. The reader now also presents retained text/choice values and checked checkbox/radio selections in its readable view. It does not modify the PDF. Retained employer answers are normalized through the same validator used for printing, including whitespace normalization, and only the chosen document branch is rendered.

Verification: nine final serial backend tests passed without skips in 23.1 seconds (/tmp/payroll-i9-employer-review-tests-final.log), including assisted/unassisted packets, canonical employee signature preservation, unsigned employer fields, signed preparer PDF retention, normalized retained/printed values, encryption, prior-review invalidation, expiry, another-admin and foreign-facility rejection, missing packet page rejection, immutable evidence, audit rollback, stale draft/page rejection at API and database levels, plus draft/route regressions. Four browser journeys passed in 1.4 minutes (/tmp/payroll-i9-employer-review-browser-final.log): employer draft retry/resume, employer packet including all four main pages and one signed preparer sheet, all eight instruction pages, readable filled values, clearing on edit, no private values in browser storage, no page errors or horizontal overflow, and ordinary/exempt W-4 signing-to-admin-application regressions. The mobile viewer was visually inspected. Scoped lint, whitespace and the final production build passed (/tmp/payroll-i9-employer-review-build-final.log), with existing chunk/data-age warnings and opt-in prerender skipped. The earlier browser failure for absent readable field values was fixed and rerun; it is not an unresolved failure.

Assumptions: each admin reviews their own current packet; unsigned previews do not attest to examination, acceptable documents, alternative-procedure qualification or consent; every preparer remains a separate retained original certification; final PDF entries come from the saved selected branch; draft/context/employee changes require new review. This does not implement employer signing or replace the existing generic completion action with native signature proof. Remaining work includes document examination and copies, alternative-procedure qualification, employer attestation/signature transactions, receipt/extension/deadline handling, continuation sheets, corrections/reverification/retention and broader onboarding/payroll/provider/production requirements. No production migration, real examination or real signature occurred.

Checkpoint 516 (internal employer I-9 drafts): Added admin-scoped employer-draft GET/POST endpoints and a private partial-entry form on the Employer I-9 review step. Admins can save/resume up to three List A rows or List B/C, switch alternatives without losing unfinished branch entries, and record partial dates, representative/employer details, additional information and an explicitly unanswered or selected examination method. Draft text is encrypted and excluded from generic checklist responses, audit summaries and browser storage. A locked basis binds the draft to the current employee Section 1 submission, employee/employer task state, cycle and hiring-context revision; changes invalidate the old draft while retaining its revision counter. Case-insensitive exact retries and concurrent identical requests create one revision; changed/stale writes are rejected. Database guards preserve scope/revision and require the current employee signature and editable employer step. An initial upsert test exposed an insert-trigger check firing before conflict resolution; the guard now distinguishes a genuinely new draft from the insert phase of an update. Nine final serial backend tests passed without skips in 23.8 seconds (/tmp/payroll-i9-employer-draft-tests-final.log), including encryption, cross-employee/facility denial, employee-token rejection, exact/concurrent retries, context invalidation, a real employee amendment invalidating the old source, direct stale-source DB rejection, audit rollback, vault-key failure and neighboring employee draft/signing/route checks. The mobile browser journey passed in 21.2 seconds (/tmp/payroll-i9-employer-draft-browser.log): simulated lost save, unchanged retry, resume, branch preservation, method selection, context invalidation, no private document number in browser storage, no page errors and no horizontal overflow. The first browser attempt timed out on an exact text-label selector for the select; the corrected role/name selector passed. The mobile form was visually inspected. Scoped lint and whitespace passed; the production build passed (/tmp/payroll-i9-employer-draft-build.log) with existing chunk/data-age warnings and opt-in prerender skipped. Assumptions: unanswered fields stay unanswered; drafts may retain both document branches but final preparation must select only the chosen branch; saving does not attest to document acceptance, examination, remote eligibility or sign Section 2; an employee amendment or reviewed-context change requires fresh employer entries. Remaining native employer work includes form preview, examination/copy evidence, remote qualification, attestation/page review/signing transactions, signatures bound to current preparer work, receipt/extension/deadline handling and continuation sheets. The existing generic employer completion action is not proof of a native employer signature. The wider onboarding/payroll/provider/production goal remains unfinished. No production migration, real document examination or real signature occurred.

Checkpoint 515 (employer Section 2 form composition): Added strict structural Section 2 inputs and a renderer that starts from the retained employee-signed four-page I-9. It captures one to three List A document rows or the exclusive List B plus C pair, issuing authorities, optional identifiers/expiration dates, additional information, explicit examination method, first employment date and representative/employer details. It leaves the employer signature/date blank for a separate authenticated signing transaction. The official Document Title 1 field binds a Section 2 widget and a Supplement B widget; the renderer detaches only the page-one widget and creates an independent canonical Section 2 title while preserving the original blank Supplement B field. It verifies all other canonical employee/supplement values remain unchanged. Existing employer-signed or already-filled employer sources are rejected instead of overwritten. Entries that cannot print legibly or need unsupported font characters are rejected without truncation; long additional information requires a continuation sheet that remains to be implemented. Seven serial PDF tests passed without skips in 9.9 seconds (/tmp/payroll-i9-section2-tests.log), including Section 1 and preparer regression cases. The initial alternative-checkbox test incorrectly expected On; the official field uses Yes, and the final check compares the widget appearance state to its canonical value. An independent pypdf check verified all 130 widgets in both synthetic List A signed and List B/C unsigned outputs against canonical values and normal appearances, resolving nested official field names. Rendered first pages and blank Supplement B were visually inspected. Scoped lint and whitespace checks passed. Assumptions: document choice is explicit and never inferred from citizenship; a form entry or alternative checkbox does not establish acceptable-document status, actual examination, remote-procedure eligibility or signature consent; employer previews start from immutable employee evidence; corrections must preserve earlier signed forms. This is not yet connected to an admin route, encrypted employer draft or completion action. Next required work is the scoped employer examination/copy-evidence and signing workflow, including current employee/preparer binding, remote qualification, deadline/receipt/extension handling, continuation sheets and UI/browser validation. Existing generic employer completion remains insufficient evidence of a native Section 2 signature. No production migration or real signature occurred; broader onboarding/payroll/provider/production requirements remain unfinished.

Checkpoint 514 (independent preparer/translator certifications): Added a separate private preparer portal at /employee/payroll/preparer, its rate-limited /api/payroll/preparer namespace, and hiring-admin invitation/roster controls on the I-9 employee step. Each invitation is scoped to a specific employee Section 1 submission, stores only a hash plus encrypted recovery material for its 32-byte random access token, expires after 14 days, and supports exact invitation retries. The URL fragment is removed on portal entry; guest access uses a distinct browser-session key and cannot authenticate to employee payroll. The portal exposes the employee name and preparer's own supplement, not employee SSN/address/DOB. A preparer must enter a name matching the intended recipient, review their one-page supplement, acknowledge the exact attestation and sign their own certification. Exact signature retries produce one retained PDF. Each person gets a separate official Supplement A sheet; five-person coverage proves there is no four-block capacity ceiling. The original template's second block binds both first/last-name widgets to one field; only the independent first block is filled per sheet, leaving the others blank. Field-tree/widget checks and rendered signed-sheet/mobile-receipt inspection passed. Signed forms and signature evidence are encrypted and immutable. Cancellation invalidates an unsigned link; signed evidence cannot be cancelled away. An actual employee amendment invalidates old preparer review/signing and excludes old certifications from the new submission's roster. Admin completion requires at least one active preparer, all active certifications signed, and an explicit current roster-completeness confirmation whose fingerprint is retained in the review response. Ten final serial backend checks passed without skips (22.8 seconds, /tmp/payroll-i9-preparer-complete-tests.log), including five independent certificates, invitation and signature retries, recipient-name mismatch, missing page review, cross-token review rejection, employee-token separation, foreign-facility denial, audit rollback, cancellation, immutable evidence, real amendment invalidation and neighboring I-9/route checks. Two final browser journeys passed (48.6 seconds, /tmp/payroll-i9-preparer-browser-final.log): existing employee signing and actual preparer-URL invitation/signing/download/admin completion, with simulated lost invitation/signature responses. Scoped lint, whitespace, backend server syntax and production build passed (/tmp/payroll-i9-preparer-build.log); existing chunk/data-age warnings and skipped opt-in prerender remain. Assumptions: intended names use first name, optional middle initial and last name; each person receives their own private link; every known assistant must be listed and individually sign; the admin explicitly verifies roster completeness; one original supplement is retained per preparer; a changed employee signature requires fresh preparer work. Identity/contact verification evidence is admin-attributed and link possession is not by itself proof of complete production identity assurance. Delivery is currently private manual handoff, with no automatic email; expired-link recovery, partial preparer drafts, broader name/script handling, corrections/inspection exports and full production identity assurance remain open. Native employer examination/signing, lifecycle/retention/current-rule verification and the full broader payroll/provider/production goal also remain unfinished. No production migration, real invitation delivery or real signature occurred.

Checkpoint 513 (employee I-9 Section 1 signing): Added explicit employee attestation/read/signing intent, four-page review gating, employee-session-scoped signing and unchanged-request retry protection. Signing modifies only the signature/date fields in the exact retained preview, verifies every other canonical field remains unchanged, retains the four official pages, encrypts signature/session evidence and stores a private signed PDF. The actual server transaction date is used; no caller-provided signing date is accepted. Successful signing clears the private draft, preserves the submission reference against generic overwrites, sets employee I-9 status to SECTION_1, resets the separate document checklist to REVERIFY and reopens employer review. A reported preparer requirement blocks employee-step approval; premature employer approval is also blocked. The employee/admin can download the retained Section 1 PDF, and the onboarding step retains its signing timestamp and confirmation reference. Nine final serial backend checks passed without skips (30.9 seconds, /tmp/payroll-i9-signing-tests-final.log), covering required consent/page visits, stale hiring context, concurrent/uppercase retries producing one signature, changed retry rejection, audit rollback including prior employer status, encrypted evidence, canonical PDF fields, cleared drafts, generic overwrite rejection, preparer/employer approval gates, immutable signature history and neighboring preview/context/draft/route regressions. The final I-9 employee/admin browser journey passed (36.8 seconds, /tmp/payroll-i9-signing-receipt-browser.log), including a synthetic lost-signing response, unchanged retry, one retained signature, cleared private fields, employee/admin PDF downloads and the explicit amendment action. Mobile review initially exposed a blank form immediately after signing; the default is now the compact retained confirmation/download view, with an explicit amendment action. An earlier combined browser run had an ordinary W-4 read-only benefits-proxy ECONNRESET (two other journeys passed); added bounded GET-only retries and guaranteed harness cleanup. Both W-4 cases then passed (1.0 minute, /tmp/payroll-i9-signing-w4-recheck.log); writes retain zero automatic proxy retries. The final build passed (/tmp/payroll-i9-signing-build-final.log). The synthetic signed PDF was rendered and visually inspected: signature/date placement and accented names are legible; employer and preparer fields remain unsigned. Scoped lint, whitespace and production build passed with existing chunk/data-age warnings and opt-in prerender skipped. Source: the retained 2025 annual 8 CFR 274a.2(h)-(i), https://www.govinfo.gov/content/pkg/CFR-2025-title8-vol1/pdf/CFR-2025-title8-vol1-part274a.pdf, requires attestation acknowledgement, signature association/time, identity evidence and printed confirmation. This annual source is not a verification of all current 2026 amendments. Assumptions: an employee signs only their Section 1; assistance never implies a preparer's signature; changes require a new employee review/signature; existing employer verification must be revisited. The retained authenticated-session record is evidence of account control, not by itself proof of complete identity-assurance or electronic-I-9 compliance. Native preparer certifications, employer examination/signing, dedicated inspection/confirmation exports, special signing cases, lifecycle/retention, current-regulation verification and broader payroll/production gaps remain active. No production migration or real signature occurred.

Checkpoint 512 (internal employee I-9 preview and page review): Connected saved employee drafts to the official four-page unsigned Section 1 PDF. Preview preparation requires the recorded admin offer/participation context and the current draft revision/hash. Encrypted immutable reviews retain the exact PDF and answers, scoped employee session, draft revision and hiring-context revision; page visits check freshness, latest preview, expiry and PDF integrity. Draft/context changes or another session cannot reuse the old review. The employee screen requires saving edits before preparation, clears its preview on editing and explicitly identifies it as unsigned. Nine serial backend checks passed without skips (17.9 seconds, /tmp/payroll-i9-preview-tests-final.log), covering context/draft invalidation, session isolation, four page visits, duplicate visit handling, encrypted storage, no private values in checklist/audit, unchanged OPEN status, immutable history, audit rollback, route boundaries and PDF field/checkbox fidelity. The final I-9 draft/admin-context/preview browser journey and both ordinary/exempt W-4 signing regressions passed (three tests, 1.6 minutes, /tmp/payroll-i9-preview-browser-final.log). The mobile preview and corrected unsigned heading were visually inspected. A PDF field-limit exception found during integration now returns an actionable validation error instead of an internal server failure. Scoped lint, whitespace and the production build passed; existing chunk/data-age warnings and skipped opt-in prerender remain. Assumptions: previews contain only saved employee answers; each new draft or admin context requires a new review; previewing or viewing pages is not a signature; employee and preparer/employer attestations must remain separate. The future signing transaction must use currentI9Review and revalidate the current basis under the same transaction locks. Remaining: native signing, preparer supplements, employer examination, lifecycle/export/current-regulation verification and broader payroll/production requirements. No production migration or real signature occurred.

Checkpoint 511 (attributable I-9 hiring-admin inputs): Added the admin I-9 preparation form and scoped GET/POST context endpoints. The admin records an accepted-offer date, explicitly selects employer/hiring-site E-Verify participation, confirms the review and supplies evidence. No participation default is selected. Sequential immutable context revisions retain the admin actor and timestamp; concurrent/stale updates and changed cycles are rejected. Transactional audit failures roll back context insertion. Six final serial backend checks passed without skips (7.9 seconds, /tmp/payroll-i9-context-tests-final.log), including foreign employee/facility scope, unauthenticated/employee-token denial, invalid/future dates, explicit confirmations, concurrent stale revision rejection, immutable history and audit rollback; neighboring I-9 draft and route checks passed. The combined employee draft/admin context browser journey passed (22.2 seconds, /tmp/payroll-i9-context-browser.log), including admin save/reload, date/participation persistence, history and confirmation reset; the mobile admin screenshot was visually inspected. Scoped lint, whitespace and the production build passed, with existing chunk/data-age warnings and opt-in prerender skipped. Testing caught and fixed SQL date serialization by returning offer acceptance as date text. Assumptions: participation is verified for this employer/hiring site and retained per onboarding cycle; accepted-offer evidence is admin-attributed; subsequent corrections append history; saving context never substitutes for employee signature or employer examination. The native signing integration must bind its preview and signature to the exact current context revision and revalidate it at submission. This context does not enroll an employer or create an E-Verify case. Native I-9 signatures, preparers, Section 2, lifecycle and the broader payroll/production goal remain unfinished. No production migration or real employee attestation occurred.

Checkpoint 510 (employee I-9 draft screen and browser verification): The employee onboarding I-9 step now supports encrypted partial draft save/resume, explicit unanswered SSN/preparer questions, all four attestation selections and conditional identifiers, unchanged retry after a lost save response, and the retained eight-page official instructions in the internal PDF viewer. Drafts do not sign or submit the I-9. The mobile browser journey passed (one test, 21.6 seconds, /tmp/payroll-i9-draft-browser.log), verifying persistence after reload, a single revision after lost-response retry, preservation through section collapse, passport/preparer fields, all eight rendered instruction pages, no horizontal overflow, no private values in browser storage/checklist/audit and task status remaining OPEN. The mobile screenshot was visually inspected. Both existing ordinary/exempt W-4 signing-to-admin-application browser regressions passed (57.0 seconds, /tmp/payroll-i9-w4-regression.log). Scoped lint, whitespace and the production build passed; the build retained existing chunk/data-age warnings and skipped opt-in prerender. Assumptions: unanswered questions stay unanswered; saving is distinct from signing; every preparer needs separate certification; employer E-Verify participation and accepted-offer confirmation must be stored as attributable inputs, not inferred. Current source search confirms those last two inputs exist only in isolated validator test contexts, so connecting their admin workflow is the next signing dependency. Remaining: native I-9 preview/signature transactions, preparer and employer signing/examination, lifecycle/export/current-rule verification and all broader full-goal gaps. No production migration or real signature occurred. This checkpoint does not establish complete onboarding or a clean rerun of the full payroll suite.

Checkpoint 509 (encrypted employee I-9 drafts): Added scoped I-9 draft GET/POST endpoints under employee authentication and the locked employee transaction wrapper. Drafts are encrypted with facility/employee/task/cycle context, resume across sessions, retain revision/retry metadata and bind to the current checklist status/response hash. A changed checklist invalidates the old draft context; stale tabs cannot overwrite it. Database guards enforce matching employee task/session, revision increments and no destructive row deletion. Draft saving does not submit the task or change employee identity. Eight final serial checks passed without skips (/tmp/payroll-i9-draft-storage-final.log, 13.9 seconds), including encrypted storage, cross-session resume, uppercase/exact/concurrent retries, stale/conflicting requests, foreign employee isolation, no-store headers, revoked sessions, no PII in checklist/audit, changed-response invalidation, database guard rejection, audit-failure rollback and missing vault-key failure; neighboring W-4/MW507 draft and route-boundary checks passed. Scoped lint and whitespace passed. Assumptions: generic checklist changes invalidate an unfinished draft context, unanswered employee questions remain unanswered, and signature intent cannot be embedded in a draft. Remaining: employee UI resume, preview/signature transactions, preparer and employer signing/examination, current-regulation verification, lifecycle and the broader full onboarding/production goal. No production migration or real signature occurred.

Source review September 12, 2026. This is implementation groundwork, not a completed I-9 workflow or a finding of regulatory compliance.

## Retained official sources

Downloaded directly from USCIS after browser retrieval returned 403; shell retrieval succeeded. Both PDFs identify edition 01/20/25. The form expires 05/31/2027.

- [Official form](https://www.uscis.gov/sites/default/files/document/forms/i-9.pdf): `backend/payroll/forms/uscis-i9-012025.pdf`, four pages, 133 AcroForm fields, SHA-256 `780f348c34df694bb0b4dbbfaf9f22b99b9757b80d16a37ba89aadf069597281`.
- [Official instructions](https://www.uscis.gov/sites/default/files/document/forms/i-9instr.pdf): `backend/payroll/forms/uscis-i9-instructions-012025.pdf`, eight pages, SHA-256 `c66c3818fbecbfb87b5f0560f4dfd4086905daf72ca29dba15ce4c8d49dc4ca3`.

Recheck the current USCIS edition before production rollout. Existing tax-form signing machinery is a possible implementation dependency; it does not establish I-9 compliance by itself.

## Required employee flow

1. Make all instructions available during completion. Section 1 follows accepted employment offer and is due by the first day of employment. Preserve actual signing time; late completion must not backdate evidence.
2. Capture legal name, other last names, address and date of birth. Preserve punctuation and the official single-name handling. Allow nonapplicable fields to remain blank. SSN is voluntary unless the employer participates in E-Verify; a pending SSN must have the instruction-supported follow-up path. Do not reuse mandatory tax-form SSN rules without checking this distinction.
3. Present all four citizenship/immigration attestations equally. Capture the identifier appropriate to the employee's chosen attestation: permanent-resident identifier, or one of the authorized-worker identifier alternatives and applicable authorization expiration. Do not infer a status from personal characteristics or documents.
4. Display the exact attestation and capture affirmative acknowledgment and the employee's own signature bound to the exact reviewed content. Keep identity, birth date and immigration identifiers encrypted and out of generic checklist responses, audit summaries, browser storage and diagnostics.
5. Ask whether a preparer/translator assisted. Every assisting person needs a distinct Supplement A certification and signature; support additional sheets without a fixed four-person limit. Never sign on another person's behalf.
6. Let the employee choose acceptable List A documentation or the List B plus C combination. Do not require a particular document as the default path.

## Required employer flow

1. Complete Section 2 within three business days after employment begins. For hires lasting fewer than three business days, it is due on the first day. The current generic three-weekday calculation is not sufficient to establish all deadline cases.
2. Capture document titles, issuing authorities, identifiers and applicable expiration dates in their original List A/B/C structure. Support multi-document List A combinations, receipt follow-ups, extensions and additional-information notations.
3. Record actual examination by the employer/authorized representative. An upload does not establish examination. An authorized alternative procedure needs its own eligibility and evidence requirements; it must not be selected automatically.
4. Bind the employer's certification, representative identity/title, first employment date, employer details and actual signature date to the retained employee Section 1. Corrections and amendments need attributable history and cannot silently replace earlier signed evidence.
5. Keep employer examination separate from E-Verify. E-Verify enrollment, cases, notices and resolution require a distinct integration and workflow. Do not claim verification from form completion alone.

## Lifecycle and electronic-system requirements

- Supplement B supports qualifying rehire within three years, reverification and name changes. Document choice for reverification remains with the employee; implement the applicable List A/C rules and exceptions from the instructions and M-274.
- Retain the completed form and applicable supplements; do not send completed I-9s to USCIS or ICE as a normal filing step.
- Before implementation signoff, verify current 8 CFR 274a.2 electronic-generation/signature/storage requirements, M-274 retention periods, inspection export, indexing, access controls, tamper detection, attribution, backups and quality-assurance procedures. These have not all been verified in this checkpoint.
- Test employee, multiple-preparer, employer, correction, receipt, extension, rehire, reverification, inspection/export and retention journeys. Narrow PDF-rendering or signature tests are not sufficient to mark internal I-9 complete.

## Current authoritative gap

`backend/payroll/onboarding.js` creates employee I9 and admin I9_REVIEW steps. `workforceRoutes.js` accepts a document/provider reference and generic admin review. There is no native I-9 draft, employee/preparer/employer signature chain, document examination workflow or Supplement B implementation yet. Retaining the official PDFs does not change that status.

## Electronic-signature source review

[Official 2025 annual CFR, 8 CFR 274a.2(e)–(i), pages 11–13](https://www.govinfo.gov/content/pkg/CFR-2025-title8-vol1/pdf/CFR-2025-title8-vol1-part274a.pdf) provides the implementation baseline below. Current eCFR retrieval returned 406; this annual edition is not asserted to establish all September 2026 changes.

- Employee signature capture must acknowledge reading the attestation, associate the signature with the completed record, apply it at the transaction, retain signer-identity verification and provide printable confirmation on request.
- Employer/representative signature capture has its own attestation requirement; implement affirmative acknowledgment for that role too.
- Preserve attributable, permanent change records and inspection access to requested forms, supporting documents, audit trails and obtainable structured summaries.
- Document creation, maintenance, authenticity and indexing processes. Verify access controls, backups/recovery, operator training and periodic quality checks before claiming electronic-system compliance.

Implementation decision: separate each signer's authenticated transaction from PDF rendering. A generic document-upload receipt or a checkbox completing the hiring checklist cannot stand in for a signer transaction or document examination. Existing encrypted-document primitives may be reused only after their identity, audit, confirmation and export coverage is proven for I-9.

## Reproducible form inventory

Run `python3 scripts/payroll/extract-i9-fields.py --check` in an environment with `pypdf`. The extractor verifies the pinned form digest and compares field-tree entries, page widget locations, inherited maximum lengths and appearance-state names to the retained JSON inventory. It passed on September 12, 2026.

The PDF contains 133 field-tree entries and 130 page widgets; a field-tree count is not a count of visible inputs. Some List A parent/child entries have no direct widget. Citizenship checkboxes use `/On` appearance names, while alternative-procedure boxes use `/Yes` and `/Off`. The eventual renderer must handle these actual structures and re-read canonical values and widget appearances after filling. This inventory does not prove that any filled or signed I-9 renders correctly.

## Section 1 structural validator groundwork

`backend/payroll/i9Section1.js` validates the pinned edition, accepted-offer/E-Verify context, employee information, actual calendar dates, optional fields, SSN/pending-SSN distinction, all four attestation branches and one authorized-worker identifier alternative. It rejects cross-branch identifier fields and signatures embedded in an answers payload. It preserves the need for preparer certification rather than treating assistance as completed.

Two standalone unit tests and scoped lint passed. These tests were added after the full payroll suite began and are not included in that running suite's count. This module is not imported by the current onboarding routes and did not change the runtime under regression testing.

Limitations to resolve before integration: I-94 and passport numbers currently receive structural text/length checks, not a definitive issuance-format validation; verify current I-94 alphanumeric guidance before tightening these rules. The validator does not establish status, authorization validity or document authenticity. It does not perform signature capture, actual employer E-Verify enrollment lookup, preparer identity verification, Section 2 examination, rendering, persistence, corrections or lifecycle handling. Authorization-expiration treatment and extensions require the separate review workflow; no historical attestation is silently rewritten.

## Unsigned Section 1 PDF preview groundwork

`i9Section1Pdf.js` fills the retained original form with a pinned embedded font, preserves all four pages, leaves employee/date/employer signatures blank, and reopens the output to check canonical text. All four attestation checkbox variants were tested for matching canonical and widget appearance states. Optional SSN stays blank. Overlong/unprintable fields fail before a PDF is returned. Two renderer tests passed (`/tmp/payroll-i9-renderer-tests.log`, 4.1 seconds); a synthetic citizen preview was rendered and visually inspected. These standalone tests are separate from the already-running full suite.

The preview is unsigned and is not connected to onboarding routes. The intended UI must clearly identify it as a preview outside the original form content. The original form language is preserved. Subsequent employee/preparer/employer signatures, document examination, private storage, confirmation and audit/export workflows still require implementation and verification.

## Partial draft input groundwork

`i9DraftInput.js` accepts unfinished employee fields, partial SSN/date entries and selected identifier alternatives. Pending-SSN and preparer-assistance answers can remain null instead of defaulting to an attestation. Signatures, signing dates, employer approval and unknown/nested fields are rejected. Two focused tests and scoped lint passed. This is input normalization only: encrypted revisioned storage, scoped sessions, retry semantics, clearing after signature and UI resume still need implementation. Its new tests are separate from the full suite already in progress.

## Draft-to-preview boundary

`i9DraftToSection1.js` converts validated draft fields into the final Section 1 input and includes only identifiers for the employee's selected attestation. Unselected alternative fields remain in the draft but do not become part of another attestation. Incomplete identity, unanswered assistance questions and missing required passport-country entries cannot become a complete preview. Two focused tests and scoped lint passed; this module remains separate from current onboarding routes and the in-progress full regression suite.
