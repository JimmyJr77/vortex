import {test,expect} from '@playwright/test'
import {carrierRemittanceAdvice,carrierRemittanceAdviceHtml} from '../../backend/payroll/carrierRemittanceAdvice.js'
test('carrier remittance advice renders carrier-facing content on mobile and print',async({page})=>{
 const advice=carrierRemittanceAdvice({id:'00000000-0000-4000-8000-000000000001',invoiceId:'00000000-0000-4000-8000-000000000002',invoiceRevision:2,sourceObservationId:1,employerName:'Synthetic Employer',carrier:'Synthetic <Benefits> & Health',invoiceNumber:'SEP-2026',amountCents:55000,paymentDate:'2026-09-18',mode:'TEST',status:'BANK_CONFIRMED',bankWithdrawals:[{postedDate:'2026-09-17',amountCents:25000},{postedDate:'2026-09-18',amountCents:30000}],observedAt:'2026-09-19T12:00:00Z'})
 await page.setViewportSize({width:390,height:1100});await page.setContent(carrierRemittanceAdviceHtml(advice));await expect(page.getByRole('heading',{name:'Carrier remittance advice',exact:true})).toBeVisible();await expect(page.locator('body')).toContainText('Synthetic <Benefits> & Health');await expect(page.locator('body')).toContainText('TEST REMITTANCE ADVICE — SYNTHETIC FUNDS');await expect(page.locator('body')).toContainText('Payment amount: $550.00');expect(await page.evaluate(()=>document.documentElement.scrollWidth<=window.innerWidth)).toBe(true);await page.screenshot({path:'/tmp/payroll-carrier-remittance-document-mobile.png',fullPage:true});await page.setViewportSize({width:1000,height:1100});await page.emulateMedia({media:'print'});await expect(page.locator('body')).toContainText('does not state that the invoice is paid in full');await page.screenshot({path:'/tmp/payroll-carrier-remittance-document-print.png',fullPage:true})
})
